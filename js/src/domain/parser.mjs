const TokenSequence = () => {
    const tokens = [];

    return {
        add: (token) => {

        },
        parse: () => {

        }
    };
};

const TreeNode = (value, parent, left, right) => {
    return {
        value: value,
        parent: parent,
        left: left,
        right: right,
    };
};

export {
    TokenSequence
};